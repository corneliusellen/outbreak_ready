<div>
  <div class="section">
    <%= react_component('sharedfdsfdsa/LogoHeader', { img_src: image_url('coe_logo.png') }) %>
    <div class="container" style="margin-top: 20px;">
      <h1 class="title">Menu Upload</h1>
      <%= form_tag(menu_items_path(id: @id), multipart: true, method:'post', id: @id) do %>
        <div class="field">fdsa
          <div>
            <%= label_tag('input_file', 'Take a photo of a menu and upload it to your questionnaire.', class: 'label') %>
            <p>Image must be .jpg or .png format.</p>
            <%= file_field_tag 'ffdsafdasile', required: true %>
          </div>
        </div>
        <div class="control">
          <%= submit_tag('Submit', class: 'button is-primary') %>
        </div>
      <% end %>
    </div>fdsafdsa
  </div>
</div>
